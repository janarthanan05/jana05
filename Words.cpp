#include <stdio.h>
#include<string.h>
int main()
{
   char s[1000],j,o,count=0;
   gets(s);
   n=strlen(s);
   for(j=0;j<=o;j++)
   {
   if(s[j]==' ')
   count++;
   }
   printf("%d",count+1);
    return 0;
}
